//Declare an integer and a string variable
//Initialize them and concatenate them in a new variable
//Print the result in the console


int number = 10;
string numberString = "20";

//if at least one of the operands is a string, the whole result when using a + will be a string
string result = number + numberString;
Console.WriteLine(result);